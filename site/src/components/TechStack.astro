---
export interface Props {
  technologies: string[];
}

const { technologies } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {technologies.map((tech) => (
    <span class="font-mono text-xs border border-rule px-3 py-1 text-muted">
      {tech}
    </span>
  ))}
</div>
