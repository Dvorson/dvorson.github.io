---
export interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="text-left">
  <p class="font-display text-3xl font-bold mb-1">{value}</p>
  <p class="font-mono text-xs tracking-wider uppercase text-muted">{label}</p>
</div>
