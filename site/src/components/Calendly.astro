---
export interface Props {
  url?: string;
  text?: string;
}

const { url = 'https://calendly.com/dvorson', text = 'Book a Call' } = Astro.props as Props;
---

<a href={url} target="_blank" rel="noopener noreferrer" class="btn-primary">
  {text}
</a>
